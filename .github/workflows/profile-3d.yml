name: GitHub-Profile-3D-Contrib

on:
  schedule: # 03:00 JST == 18:00 UTC
    - cron: "0 18 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: generate-github-profile-3d-contrib
    steps:
      - uses: actions/checkout@v3
      - uses: yoshi389111/github-profile-3d-contrib@0.7.1
        env:
          GITHUB_TOKEN: ${{ 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 }}
          USERNAME: ${{ Quocdev03 }}
      - name: Commit & Push
        run: |
          git config user.name Quocdev03
          git config user.email ccquoc2100346@student.ctuet.edu.vn
          git add -A .
          git commit -m "generated"
          git push
